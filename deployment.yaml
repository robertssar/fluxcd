apiVersion: apps/v1
kind: Deployment
metadata:
  name: getting-started
  namespace: default
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  replicas: 5
  selector:
    matchLabels:
      app: getting-started
  template:
    metadata:
      labels:
        app: getting-started
    spec:
      containers:
      - name: getting-started
        image: fluxcdstudent20.azurecr.io/fluxexamples/getting-started:flux
        ports:
        - containerPort: 4000
      imagePullSecrets:
      - name: acr-secret